# Lean runtime for deployed FastAPI (JSON fallback + Groq Ask AI).
# Avoids sentence-transformers / torch — unsuitable for free-tier hosts.
# Full local/dev stack remains in requirements.txt.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
pgvector==0.3.6
httpx==0.28.1
groq==0.13.1
openpyxl==3.1.5
pandas==2.2.3
