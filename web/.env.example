# Vercel production: use same-origin /api (rewritten to Render in vercel.json).
# This avoids browser CORS issues.
VITE_API_BASE=/api

# Direct-to-Render alternative (requires CORS_ORIGINS on Render):
# VITE_API_BASE=https://myntra-ai-discovery-engine.onrender.com
